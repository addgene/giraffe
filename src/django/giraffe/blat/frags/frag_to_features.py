from giraffe.blat.models import Sequence
from giraffe.blat.models import Sequence_Feature
from giraffe.blat.models import Feature

def get_features_from_frags(frags):
    """
    Given a list of feature fragments, turns them into features.

    Each member of the feature fragment list is a string with 4 numbers:
    feature index
    fragment index
    start position
    shift
    """

    print "Turning frags into features"

    return 1
